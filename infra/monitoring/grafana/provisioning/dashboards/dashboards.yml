apiVersion: 1

providers:
  - name: 'PRISMA Dashboards'
    orgId: 1
    folder: 'PRISMA'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
